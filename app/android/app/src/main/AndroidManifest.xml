<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="rotacusto_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Warning: Do not change service name (nome fixo do plugin
             flutter_foreground_task) — é isso que mantém a navegação viva
             com a tela apagada ou o app trocado por outro (Fase 6.3). -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- API do back-end é HTTP/HTTPS puro (dio) — sem isso, todo request de
         rede falha silenciosamente com SocketException no Android. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Navegação turn-by-turn (Fase 6) — posição ao vivo pra seguir a rota.
         COARSE é pedida junto porque alguns fabricantes/versões negam FINE mas
         concedem COARSE; o app funciona com precisão reduzida nesse caso. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Navegação em segundo plano (Fase 6.3): ACCESS_BACKGROUND_LOCATION é
         declarada aqui mas só PEDIDA em runtime como um passo separado, depois
         da foreground já concedida (o Android 11+ só oferece "permitir
         sempre" quando solicitada assim — ver PermissaoSegundoPlanoScreen).
         FOREGROUND_SERVICE/WAKE_LOCK/POST_NOTIFICATIONS já vêm do manifesto
         próprio do plugin flutter_foreground_task, não precisam repetir aqui. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
